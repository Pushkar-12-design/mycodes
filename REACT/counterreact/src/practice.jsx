export const Practice=() =>
{
    const students =[];
    return (
    <div>
        <p> {students.length ===0&& "No students found"}</p>
            <p> Number of students : {students.length}</p>
        
    </div>
);};